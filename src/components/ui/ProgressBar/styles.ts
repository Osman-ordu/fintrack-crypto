import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
    container: {
      width: '100%',
      marginBottom: 24,
    },
    track: {
      height: 6,
      borderRadius: 3,
      overflow: 'hidden',
    },
    progress: {
      height: '100%',
      borderRadius: 3,
    },
  });