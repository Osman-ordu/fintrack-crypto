import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
  title: {
    marginBottom: 6,
    textAlign: 'center',
  },
  subtitle: {
    marginBottom: 0,
    textAlign: 'center',
    opacity: 0.7,
  },
});

